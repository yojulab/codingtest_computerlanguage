### pre langague study for python
* python
 * input()
 * print()
 * int(), float(), str()
 * str.format() : {:>8}
 * str.split(), replace() 
 * list(), dict(), set()
 * type(), len()
 * if, while, for
 * range(), enumerate()
 * and, or, not
 * map()
 * zip()
 * re